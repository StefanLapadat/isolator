{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Rust Program",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/web-api/target/debug/web-api.exe",
            "args": [],
            "cwd": "${workspaceFolder}/web-api",
            //"preLaunchTask": "cargo build",
            "sourceLanguages": ["rust"]
        }
    ]
}
